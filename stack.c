#include "monty.h"
/**
 * f_stack - it prints the top of the stack
 * @head: the stack head
 * @counter: line number
 * Return: no return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
